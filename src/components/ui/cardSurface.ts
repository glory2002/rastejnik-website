/** Shared surface for cards whose background turns white on hover/focus. */
export const cardSurfaceClass = "card-surface";

/** Gap between sibling cards — standard grids, flex rows, and lists. */
export const cardGapClass = "gap-card";

/** Tighter gap for nested or many-up card groups. */
export const cardGapCompactClass = "gap-card-compact";
