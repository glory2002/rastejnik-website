/** Shared surface for cards whose background turns white on hover/focus. */
export const cardSurfaceClass = "card-surface";

/** Regular card grid — column + row gutters from the spacing tokens. */
export const cardGapClass = "gap-x-card gap-y-card-row";

/** Featured cards on one row (or stacked). */
export const cardGapFeaturedClass = "gap-card-featured";

/** Space between a featured row and the regular card grid below. */
export const cardGapHierarchyClass = "gap-card-hierarchy";

/** Tighter gap for nested or many-up card groups. */
export const cardGapCompactClass = "gap-card-compact";
